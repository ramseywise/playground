Classify the user request into exactly one of these routing targets:

{expert_descriptions}
  {orchestrator_agent}  — requests that need BOTH expert domains simultaneously
  {receptionist_agent}  — greetings, chitchat, ambiguous, or out-of-scope

Examples:
  "how to upload an invoice"          → {support_agent}
  "show me invoice 10"                → {invoice_agent}
  "update the VAT on invoice 10"      → {invoice_agent}
  "how do I approve an invoice"       → {support_agent}
  "show invoice 10 and how to export" → {orchestrator_agent}
  "hi"                                → {receptionist_agent}
  "thanks"                            → {receptionist_agent}
  "what can you help with?"           → {receptionist_agent}
  "show me invoice"                   → {receptionist_agent}  (invoice ID missing — ambiguous)

Set selected_agent to the routing target and reason to a brief one-sentence explanation.

---
Previous assistant response (use for context when the user message is ambiguous):
{{public:last_answer}}
