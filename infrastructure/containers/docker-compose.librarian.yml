services:
  librarian-api:
    build:
      context: ../..
      dockerfile: infrastructure/containers/api/Dockerfile
    ports:
      - "8000:8000"
    env_file: ../../.env
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/health').raise_for_status()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s  # multilingual-e5-large loads ~45s on cold start

  frontend:
    build:
      context: ../..
      dockerfile: infrastructure/containers/frontend/Dockerfile
    ports:
      - "8501:8501"
    environment:
      LIBRARIAN_API_URL: http://librarian-api:8000
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8501/_stcore/health').raise_for_status()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    depends_on:
      librarian-api:
        condition: service_healthy

  eval-dashboard:
    build:
      context: ../..
      dockerfile: infrastructure/containers/eval-dashboard/Dockerfile
    ports:
      - "8502:8502"
    environment:
      LANGFUSE_ENABLED: "${LANGFUSE_ENABLED:-false}"
      LANGFUSE_PUBLIC_KEY: "${LANGFUSE_PUBLIC_KEY:-}"
      LANGFUSE_SECRET_KEY: "${LANGFUSE_SECRET_KEY:-}"
      LANGFUSE_HOST: "${LANGFUSE_HOST:-https://cloud.langfuse.com}"
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8502/_stcore/health').raise_for_status()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

  jaeger:
    image: jaegertracing/all-in-one:1.57
    profiles:
      - tracing
    ports:
      - "16686:16686"  # Jaeger UI
      - "4317:4317"    # OTLP gRPC ingest
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:14269/"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
